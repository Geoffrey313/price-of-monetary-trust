# Pinned to the versions used to produce the published numbers. The pooled
# bootstrap, the HAC/HC3 covariances and linearmodels are version-sensitive, so
# the SHA-256 reproduction check only holds against these exact versions.
# Tested on CPython 3.12.
pandas==2.2.3
numpy==2.4.3
scipy==1.17.1
matplotlib==3.10.8
statsmodels==0.14.6
linearmodels==7.0
xlrd==2.0.2
pyarrow==23.0.1
requests==2.29.0
pydantic==2.13.4
pydantic-settings==2.14.1
wrds==3.5.0
